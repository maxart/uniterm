# Uniterm vs Herdr performance and assurance report

> Generated by `ut-compare 0.2.0` from 1 isolated run(s). Results are comparative measurements, not universal constants.

## Executive summary

- **linux / x86_64 (xps):** Uniterm (ut) ranks first on the balanced performance index (100.0/100). The index normalizes only the core measured metrics listed below; inspect the raw table before making a claim.
- **Privacy assurance:** Uniterm (ut) has the highest evidence-weighted checklist score (91.7/100). This is a transparent review rubric, not a penetration test or certification.
- **Security assurance:** Uniterm (ut) has the highest evidence-weighted checklist score (100.0/100). This is a transparent review rubric, not a penetration test or certification.

## Test identity and comparability

| Host | Profile | Kernel | Architecture | WSL | Contender revisions |
| --- | --- | --- | --- | --- | --- |
| linux / x86_64 (xps) | smoke | Linux 7.1.9-arch1-2 | x86_64 | no | Uniterm (ut) `2a6da6bf370d`<br>Herdr `9eb521456ac0` |

The harness enforces these controls:

- Terminal latency and output completion use a real PTY for both apps.
- Control APIs are used only for readiness, status probes, and teardown.
- Each app receives a private HOME and XDG tree; Herdr update and manifest checks are disabled during timing.
- Startup is timed until an identical pane-listing probe succeeds; detached idle is sampled after one attach/detach so both servers hold exactly one pane shell.
- Herdr's headless render grid is pinned to the attached geometry.
- Every latency and output trial is verified against a modelled screen; a wrong or incomplete final screen fails the metric instead of scoring as fast.
- Resize-storm (over the output-workload scrollback), multi-client (fresh session), pane-scaling/recovery (fresh session), and restart scenarios are reported as context; they are not folded into the balanced index.
- CPU percent is one-core percentage from cumulative process CPU time, not percentage of the whole machine.
- Root metrics cover server and attached client roots; cohort metrics also include their descendants.
- Geometry is 120x40; the live workload uses 4 panes and 1000 deterministic output lines.

## Performance results

### linux / x86_64 (xps)

| Rank | Contender | Balanced index | Core metrics present |
| ---: | --- | ---: | ---: |
| 1 | Uniterm (ut) | 100.0 | 8/8 |
| 2 | Herdr | 39.2 | 8/8 |

| Metric | Uniterm (ut) | Herdr | Winner |
| --- | ---: | ---: | --- |
| `server_startup_ready` (ms) | 8.93 | 47.96 | Uniterm (ut) |
| `control_command_latency` (ms) | 1.86 | 4.36 | Uniterm (ut) |
| `daemon_idle_cohort_cpu` (% core) | 0.000 | 0.000 | tie (within 1%) |
| `daemon_idle_cohort_rss` (KiB) | 10.91 MiB | 24.18 MiB | Uniterm (ut) |
| `foreground_idle_cohort_cpu` (% core) | 0.000 | 0.494 | Uniterm (ut) |
| `foreground_idle_cohort_rss` (KiB) | 15.57 MiB | 36.27 MiB | Uniterm (ut) |
| `terminal_input_to_visible` (ms) | 2.09 | 6.24 | Uniterm (ut) |
| `terminal_output_completion` (ms) | 18.79 | 35.46 | Uniterm (ut) |
| `binary_size` (bytes) | 5.64 MiB | 21.99 MiB | Uniterm (ut) |
| `client_render_output` (bytes) | 2.8 KiB | 4.8 KiB | Uniterm (ut) |
| `daemon_idle_root_cpu` (% core) | 0.000 | 0.000 | tie (within 1%) |
| `daemon_idle_root_rss` (KiB) | 6.64 MiB | 19.95 MiB | Uniterm (ut) |
| `foreground_idle_root_cpu` (% core) | 0.000 | 0.494 | Uniterm (ut) |
| `foreground_idle_root_rss` (KiB) | 11.22 MiB | 31.97 MiB | Uniterm (ut) |
| `isolated_state_size` (bytes) | 3.2 KiB | 4.3 KiB | not ranked |
| `live_suite_shutdown` (ms) | 48.02 | 78.95 | Uniterm (ut) |
| `multiclient_3_idle_cohort_cpu` (% core) | 0.000 | 0.493 | Uniterm (ut) |
| `multiclient_3_idle_cohort_rss` (KiB) | 24.60 MiB | 58.90 MiB | Uniterm (ut) |
| `multiclient_3_idle_root_cpu` (% core) | 0.000 | 0.493 | Uniterm (ut) |
| `multiclient_3_idle_root_rss` (KiB) | 20.28 MiB | 54.58 MiB | Uniterm (ut) |
| `multiclient_3_input_to_visible` (ms) | 2.12 | 10.49 | Uniterm (ut) |
| `multipane_4_idle_cohort_cpu` (% core) | 0.000 | 0.493 | Uniterm (ut) |
| `multipane_4_idle_cohort_rss` (KiB) | 28.84 MiB | 49.41 MiB | Uniterm (ut) |
| `multipane_4_idle_root_cpu` (% core) | 0.000 | 0.493 | Uniterm (ut) |
| `multipane_4_idle_root_rss` (KiB) | 11.63 MiB | 32.30 MiB | Uniterm (ut) |
| `multipane_process_count` (processes) | 6 | 6 | not ranked |
| `multipane_suite_shutdown` (ms) | 46.71 | 78.92 | Uniterm (ut) |
| `pane_close_recovery` (%) | 98.1 | 91.9 | Uniterm (ut) |
| `pane_close_rss` (KiB) | 15.96 MiB | 36.55 MiB | Uniterm (ut) |
| `pane_memory_slope` (KiB/pane) | 4478 | 4744 | Uniterm (ut) |
| `resize_storm_cpu` (ms CPU) | 0.00 | 50.00 | Uniterm (ut) |
| `resize_storm_rss` (KiB) | 21.28 MiB | 38.62 MiB | Uniterm (ut) |
| `resize_storm_settle` (ms) | 4.70 | 250.03 | Uniterm (ut) |
| `restart_ready` (ms) | 10.24 | 53.67 | not ranked |
| `server_shutdown` (ms) | 46.37 | 305.00 | Uniterm (ut) |
| `terminal_output_ingest_rate` (MiB/s) | 3.701 | 1.936 | Uniterm (ut) |

## Security and privacy assessment

Checklist scores use pass=100, partial=50, fail=0. Unknown and not-applicable items are excluded, and every criterion has the same definition for every contender. Static code counts do not affect these scores.

| Category | Uniterm (ut) | Herdr |
| --- | ---: | ---: |
| Privacy | 91.7 | 83.3 |
| Security | 100.0 | 81.2 |

### Privacy: Clipboard and host-data access

- **Uniterm (ut) - pass:** Clipboard export uses OSC 52 for explicit selection/yank actions, including configured copy-on-select; file and URL actions are user initiated.
  - Evidence: `crates/uniterm-server/src/copymode.rs`
  - Evidence: `README.md`
- **Herdr - pass:** Clipboard/image reads are bounded and tied to explicit paste/copy interactions; remote clipboard bridging is an explicit action.
  - Evidence: `src/server/clipboard_image.rs`
  - Evidence: `src/platform/macos.rs`
  - Evidence: `src/protocol/wire.rs`

### Privacy: Default outbound network activity

- **Uniterm (ut) - pass:** No first-party HTTP client or background update/manifest fetcher is present; the only socket use is a loopback liveness probe for user-detected development servers.
  - Evidence: `Cargo.toml`
  - Evidence: `Cargo.lock`
  - Evidence: `crates/uniterm-server/src/dev_server.rs`
- **Herdr - partial:** Version and agent-manifest checks are enabled by default and contact Herdr-controlled endpoints (product announcements ride on the version manifest); the benchmark disables both to remove network variance.
  - Evidence: `src/config/model.rs`
  - Evidence: `src/app/mod.rs`
  - Evidence: `src/update.rs`
  - Evidence: `src/update.rs`

### Privacy: Local data retention

- **Uniterm (ut) - partial:** Workspace snapshots, event logs, the Workspace catalog, and run/instruction/artifact ledgers are persisted locally with owner-only modes and are not encrypted; the README now documents the state directory as sensitive.
  - Evidence: `README.md`
  - Evidence: `crates/uniterm-server/src/persist.rs`
  - Evidence: `crates/uniterm-server/src/eventlog.rs`
  - Evidence: `crates/uniterm-server/src/workspace_catalog.rs`
- **Herdr - partial:** Session metadata and optional screen history are persisted locally as readable JSON; there is no at-rest encryption.
  - Evidence: `src/persist/io.rs`
  - Evidence: `docs/next/website/src/content/docs/session-state.mdx`

### Privacy: Network controls

- **Uniterm (ut) - pass:** The application has no background network feature requiring an opt-out in the reviewed build.
  - Evidence: `Cargo.toml`
  - Evidence: `Cargo.lock`
  - Evidence: `crates/uniterm-core/tests/no_forbidden_deps.rs`
- **Herdr - pass:** Users can disable version and agent-manifest background checks independently in configuration.
  - Evidence: `src/config/model.rs`
  - Evidence: `docs/next/website/src/content/docs/configuration.mdx`

### Privacy: Remote data path

- **Uniterm (ut) - pass:** Remote attach uses the user's own SSH connection to run the remote binary; no vendor relay or cloud path is implemented.
  - Evidence: `crates/uniterm-cli/src/remote.rs`
  - Evidence: `docs/14-ssh-remote-sessions.md`
- **Herdr - pass:** Remote access uses the user's SSH connection without a vendor relay service.
  - Evidence: `src/remote/attach.rs`
  - Evidence: `docs/next/website/src/content/docs/persistence-remote.mdx`

### Privacy: Telemetry and analytics

- **Uniterm (ut) - pass:** No telemetry or analytics sender was found in first-party source or dependencies; the core crate test forbids HTTP client crates.
  - Evidence: `Cargo.toml`
  - Evidence: `Cargo.lock`
  - Evidence: `crates/uniterm-core/tests/no_forbidden_deps.rs`
- **Herdr - pass:** No telemetry or analytics submission was found; network features fetch product-controlled manifests and releases rather than sending usage events.
  - Evidence: `src/update.rs`
  - Evidence: `src/detect/manifest_update.rs`
  - Evidence: `src/product_announcements.rs`

### Security: Child-process containment

- **Uniterm (ut) - pass:** Pane children run in dedicated process groups; event-driven retirement applies TERM, a bounded grace period, KILL, and kernel exit reaping. Runtime shutdown remains bounded.
  - Evidence: `crates/uniterm-server/src/pty.rs`
  - Evidence: `crates/uniterm-server/src/server/orchestration.rs`
  - Evidence: `crates/uniterm-server/src/runtime.rs`
  - Evidence: `crates/uniterm-server/src/server/reaper.rs`
- **Herdr - pass:** Unix process-group termination and Windows Job Object handling are platform-isolated, with explicit child termination and server handoff handling.
  - Evidence: `src/pty/actor/unix.rs`
  - Evidence: `src/platform/linux.rs`
  - Evidence: `src/platform/macos.rs`
  - Evidence: `src/platform/windows.rs`

### Security: Crash-safe persistence

- **Uniterm (ut) - pass:** Snapshots use atomic replacement; event and catalog writes are ordered and acknowledged, with failed writes preventing unsafe checkpoint advancement. Instruction and artifact ledgers remain append-only.
  - Evidence: `crates/uniterm-server/src/persist.rs`
  - Evidence: `crates/uniterm-server/src/eventlog.rs`
  - Evidence: `crates/uniterm-server/src/workspace_catalog.rs`
  - Evidence: `crates/uniterm-server/src/runtime/persistence.rs`
- **Herdr - pass:** Session and plugin registry JSON writes use adjacent temporary files and rename, cleaning temporary files on replacement failure.
  - Evidence: `src/persist/io.rs`
  - Evidence: `src/persist/plugin_registry.rs`

### Security: Extension trust boundary

- **Uniterm (ut) - pass:** The reviewed build has no plugin marketplace or third-party hook execution; provider detection reads declarative manifests, and external automation goes through the owner-only control API rather than in-process extensions.
  - Evidence: `docs/03-system-architecture.md`
  - Evidence: `docs/19-control-api.md`
  - Evidence: `docs/22-provider-detection-manifests.md`
  - Evidence: `crates/uniterm-server/src/providers/mod.rs`
- **Herdr - partial:** Plugins run out of process and installs show a trust preview, but enabled plugins intentionally execute arbitrary manifest commands and hooks with user authority.
  - Evidence: `src/plugin_command.rs`
  - Evidence: `src/app/api/plugins/runtime.rs`
  - Evidence: `docs/next/website/src/content/docs/plugins.mdx`

### Security: Local IPC access control

- **Uniterm (ut) - pass:** Runtime directories are forced to 0700 and both the Workspace socket and the per-Workspace control-API socket are chmod 0600 after bind, with stale cleanup that validates socket type and identity; there is no peer-credential check beyond file permissions.
  - Evidence: `crates/uniterm-server/src/server/socket.rs`
  - Evidence: `crates/uniterm-server/src/control_api.rs`
  - Evidence: `crates/uniterm-server/tests/rename_session.rs`
- **Herdr - pass:** Both API and client Unix sockets are explicitly reduced to owner read/write mode (0600), with stale-socket identity handling.
  - Evidence: `src/server/socket_paths.rs`
  - Evidence: `src/api/server.rs`
  - Evidence: `src/ipc.rs`

### Security: Memory-safety boundary

- **Uniterm (ut) - pass:** First-party implementation is Rust; platform unsafe is concentrated in small PTY, process-watch, client-terminal, and detached-spawn wrappers (the added openpty shim is Android-only).
  - Evidence: `crates/uniterm-core/tests/no_forbidden_deps.rs`
  - Evidence: `crates/uniterm-server/src/process_watch.rs`
  - Evidence: `crates/uniterm-server/src/pty.rs`
  - Evidence: `crates/uniterm-cli/src/lib.rs`
- **Herdr - partial:** Most product code is Rust, but terminal emulation crosses a large generated C ABI and first-party unsafe wrapper surface into vendored Zig Ghostty code.
  - Evidence: `src/ghostty/mod.rs`
  - Evidence: `src/ghostty/bindings.rs`
  - Evidence: `vendor/libghostty-vt/`

### Security: Remote exposure and authentication

- **Uniterm (ut) - pass:** Local control stays on owner-only Unix sockets; the new remote attach delegates transport and authentication to the user's OpenSSH client, and detected development URLs open only by explicit user action. No listening network control API was found.
  - Evidence: `crates/uniterm-server/src/server/socket.rs`
  - Evidence: `crates/uniterm-cli/src/remote.rs`
  - Evidence: `docs/14-ssh-remote-sessions.md`
  - Evidence: `crates/uniterm-client/src/actions.rs`
- **Herdr - pass:** Remote attach delegates transport and authentication to OpenSSH; local control remains on owner-only sockets rather than a public TCP listener.
  - Evidence: `src/remote/attach.rs`
  - Evidence: `src/remote/host_unix.rs`
  - Evidence: `docs/next/website/src/content/docs/persistence-remote.mdx`

### Security: Untrusted protocol input bounds

- **Uniterm (ut) - pass:** Direction-specific frame caps are enforced before buffer growth, and the server bounds pending bytes, clients, accepts, and messages processed per readiness turn.
  - Evidence: `crates/uniterm-proto/src/lib.rs`
  - Evidence: `crates/uniterm-server/src/server/io.rs`
  - Evidence: `crates/uniterm-server/src/control_api.rs`
- **Herdr - pass:** Client frames, initial JSON requests, graphics streams, and clipboard/image payloads have explicit size caps and private protocol version negotiation. The later stable-endpoint compatibility layer is absent from this release tag.
  - Evidence: `src/protocol/wire.rs`
  - Evidence: `src/api/server.rs`
  - Evidence: `src/api/server/pane_graphics_stream.rs`

### Security: Update integrity

- **Uniterm (ut) - not applicable:** The reviewed build has no self-update or background download implementation; installation is an out-of-product HTTPS-only script that verifies a published SHA-256, so there is no in-product update trust path to score.
  - Evidence: `Cargo.toml`
  - Evidence: `crates/`
  - Evidence: `install.sh`
  - Evidence: `install.sh`
- **Herdr - partial:** Downloaded stable binaries now require a SHA-256 from the manifest and are atomically installed, but the expected hash is delivered by the same HTTPS manifest and no independent signature verification was found.
  - Evidence: `src/update.rs`
  - Evidence: `src/update.rs`
  - Evidence: `src/update.rs`
  - Evidence: `src/checksum.rs`

## Static engineering context

| Context metric | Uniterm (ut) | Herdr |
| --- | ---: | ---: |
| First-party Rust lines | 52708 | 99144 |
| Rust test lines | 25741 | 142018 |
| English documentation lines | 4800 | 8114 |
| Cargo.lock packages | 80 | 267 |
| Direct dependencies | 18 | 25 |
| Lexical unsafe blocks | 38 | 270 |
| Lexical production unwrap calls | 30 | 1035 |

These counts describe implementation surface, not product quality. Herdr's vendored Ghostty implementation and generated bindings are excluded; Uniterm's external crates are also represented only through lockfile counts.

## Claim guidance and limitations

Safe marketing use requires all of the following:

- Publish the raw JSON, tool revision, app revisions, profile, host details, and exact binaries or hashes.
- Say "in our measured workload" and name the OS/hardware. Do not generalize one machine to every installation.
- Use the `marketing` profile for public CPU claims; smoke results validate the harness but are too short for stable idle-CPU claims.
- Repeat on native Linux, WSL2, Intel/Apple Silicon macOS as relevant, with at least three runs per host and alternating product order.
- Do not describe the assurance checklist as an audit, certification, exploit test, or guarantee.
- Re-run after either binary, configuration, dependency lockfile, or benchmark tool changes.

Known limitations:

- PTY latency intentionally includes the same `/bin/sh` scheduling and rendering path for both products; it is end-to-end, not an internal parser microbenchmark.
- RSS is sampled from `ps`; allocator-resident versus reclaimable memory is not separated.
- CPU resolution and background system noise limit very short runs.
- Feature breadth is not folded into the performance score. A faster result does not imply feature parity.
- Manual assurance findings are point-in-time source review and must be revisited when implementations change.
